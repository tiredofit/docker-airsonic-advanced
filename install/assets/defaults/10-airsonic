#!/usr/bin/with-contenv bash

AIRSONIC_USER=${AIRSONIC_USER:-"airsonic"}
AIRSONIC_GROUP=${AIRSONIC_GROUP:-"airsonic"}
CONFIG_PATH=${CONFIG_PATH:-"/config/"}
LISTEN_PORT=${LISTEN_PORT:-"4040"}
LISTEN_IP=${LISTEN_IP:-"0.0.0.0"}
URL_PATH=${URL_PATH:-""}
EXTRA_OPTIONS=${EXTRA_OPTIONS:-""}
